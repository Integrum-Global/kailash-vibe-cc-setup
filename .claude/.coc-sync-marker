# COC Sync Marker — kailash-coc-claude-py (py lane, cc-only)
timestamp: 2026-08-17T11:45:00Z
source: loom/.claude
loom_sha: f40f9588
variant: py
clis: ["claude"]
visibility: public
scope_this_cycle: content+sdk-pins   # cc-only-legacy: content axis + Steps 9-10; Steps 6.5/6.6/6.7 NOT APPLICABLE
stats:
  engine_changed: 266
  verify_failures: 0    # sync-tier-aware --verify CONSISTENT (2689 in-plan files)
  version_stamp: "loom-tracking -> 2.45.0 @ f40f9588"
  baselines_emitted: { cc: 0, codex: 0, gemini: 0 }
  cli_artifacts: none
  coc_artifacts: none
sdk_pins:
  source: build.py (kailash-py @ pyproject.toml)
  bumped:
    kailash-mcp: ">=0.4.3 -> >=0.5.1"
  NOT_bumped:
    kailash: ">=2.62.0 held — 2.63.0 is the BUILD repo's NEXT version, UNPUBLISHED"
  lock: "uv lock re-resolved, exit 0, 298 packages; exactly ONE version line moved"
preserved: [CLAUDE.md, settings.json, "visibility:public"]
note: |
  Gate-2 claude-py sync (cc-only lane) — the xref-integrity release. Third of nine
  surfaces from ONE corpus.

  RE-CUT TWICE, and both refusals were this repo's guard working correctly.

  FIRST refusal: two audit fixtures carried an SDK-source path as narrative colour
  in a probe scenario — not a citation, nothing resolving against it. Fixed AT LOOM
  and re-cut rather than allowlisted here, because this repo's guard states its own
  criterion (allowlist only where a file legitimately needs SDK source: provenance
  lines, compliance tracking, navigation skills) and incidental prose meets none of
  them. Allowlisting would have recorded a loom defect as a downstream exception at
  four consumers.

  SECOND refusal: the replacement marker QUOTED the guard's own error message
  verbatim — and that quote contains the very token the guard matches on. This
  marker ships and is scanned, so the marker became the contamination it described.
  Recorded because it is a genuinely non-obvious trap: prose ABOUT a forbidden
  token is indistinguishable, to a lexical scanner, from the token itself. This
  entry therefore describes both refusals without reproducing either literal.

  DERIVED TREES: NOT APPLICABLE, verified against this repo's own git rather than
  inferred — AGENTS.md, GEMINI.md, .codex/, .gemini/, .coc/ are all untracked here.
  Steps 6.5/6.6/6.7 NOT APPLICABLE, not skipped.

  SDK PINS — one real bump, one apparent drift that was not real. `uv lock` refused
  a bump to the BUILD repo's declared `version`, because that field is its NEXT
  version and is unpublished; reading it as the pin target was a
  producer-field-vs-reader-question error. Only kailash-mcp moved, lock REGENERATED
  rather than hand-edited.

  DISCLOSURE: CLEAN on the loom source tree and CLEAN on this staged worktree.
