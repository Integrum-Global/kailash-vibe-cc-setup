# COC Sync Marker — kailash-coc-claude-py (py lane, cc-only)
timestamp: 2026-08-17T11:05:00Z
source: loom/.claude
loom_sha: f3513659
variant: py
clis: ["claude"]
visibility: public
scope_this_cycle: content+sdk-pins   # cc-only-legacy: .claude/ content axis + Steps 9-10; Steps 6.5/6.6/6.7 NOT APPLICABLE
stats:
  engine_changed: 266   # engine file(s) applied; base origin/main c2d660213fc1
  verify_failures: 0    # stage engine sync-tier-aware --verify CONSISTENT (2689 in-plan files)
  version_stamp: "loom-tracking -> 2.45.0 @ f3513659"
  baselines_emitted: { cc: 0, codex: 0, gemini: 0 }
  cli_artifacts: none
  coc_artifacts: none
sdk_pins:
  source: build.py (terrene-foundation/kailash-py @ pyproject.toml)
  bumped:
    kailash-mcp: ">=0.4.3 -> >=0.5.1"
  NOT_bumped:
    kailash: ">=2.62.0 held — see note; 2.63.0 is UNPUBLISHED"
  lock: "uv lock re-resolved, exit 0, 298 packages; exactly ONE version line moved (kailash-mcp 0.4.3 -> 0.5.1)"
preserved: [CLAUDE.md, settings.json, "visibility:public"]
note: |
  loom@43167a54->f3513659 Gate-2 claude-py sync (cc-only lane) — the
  xref-integrity release. Third of nine Gate-2 surfaces from ONE corpus.

  DERIVED TREES: NOT APPLICABLE, verified rather than assumed. `clis: ["claude"]`
  makes this cc-only-legacy; checked against the target's own git rather than
  inferred from the declaration — AGENTS.md, GEMINI.md, .codex/, .gemini/ and
  .coc/ are ALL untracked here. Steps 6.5/6.6/6.7 are NOT APPLICABLE, not skipped.

  SDK PINS — and a correction worth recording, because the instrument caught an
  error the eye did not. Comparing this template's pins against the BUILD repo
  showed two apparent drifts: `kailash` 2.62.0 vs 2.63.0, and `kailash-mcp` 0.4.3
  vs 0.5.1. Both were bumped, and `uv lock` REFUSED:

    Because only kailash<=2.62.0 is available and your project depends on
    kailash>=2.63.0, we can conclude that your project's requirements are
    unsatisfiable.

  The BUILD repo's `version = "2.63.0"` is its NEXT version, not a PUBLISHED one.
  Reading it as "the version to pin to" was a producer-field-vs-reader-question
  error (`instrument-discipline.md` MUST-4): the field's semantics are fixed by
  its producer. `kailash` is therefore held at >=2.62.0 and the drift is NOT real.

  `kailash-mcp>=0.5.1` IS published, and only that one moved: `uv lock` exit 0,
  298 packages resolved, exactly one version line changed in the lock. The lock
  was REGENERATED rather than hand-edited — a hand-edited lock file is consistent
  with nothing and fails at install time rather than at commit time.

  DISCLOSURE: scan CLEAN on the loom SOURCE tree (4331 files, 0 findings) and
  CLEAN on the staged worktree (2680 files, 0 findings).

  PROVENANCE: restamped to f3513659 before finalize. visibility=public preserved.
