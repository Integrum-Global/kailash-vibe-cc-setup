# COC Sync Marker — kailash-coc-claude-py (py lane, cc-only)
timestamp: 2026-08-17T11:35:00Z
source: loom/.claude
loom_sha: f40f9588
variant: py
clis: ["claude"]
visibility: public
scope_this_cycle: content+sdk-pins   # cc-only-legacy: content axis + Steps 9-10; Steps 6.5/6.6/6.7 NOT APPLICABLE
stats:
  engine_changed: 266   # base origin/main c2d660213fc1
  verify_failures: 0    # sync-tier-aware --verify CONSISTENT (2689 in-plan files)
  version_stamp: "loom-tracking -> 2.45.0 @ f40f9588"
  baselines_emitted: { cc: 0, codex: 0, gemini: 0 }
  cli_artifacts: none
  coc_artifacts: none
sdk_pins:
  source: build.py (terrene-foundation/kailash-py @ pyproject.toml)
  bumped:
    kailash-mcp: ">=0.4.3 -> >=0.5.1"
  NOT_bumped:
    kailash: ">=2.62.0 held — 2.63.0 is the BUILD repo's NEXT version, UNPUBLISHED"
  lock: "uv lock re-resolved, exit 0, 298 packages; exactly ONE version line moved"
preserved: [CLAUDE.md, settings.json, "visibility:public"]
note: |
  Gate-2 claude-py sync (cc-only lane) — the xref-integrity release. Third of nine
  surfaces from ONE corpus.

  RE-CUT. The first attempt at this surface was REFUSED by this repo's own guard:

    ERROR: src/kailash/ references found in files not on allowlist:
      .claude/audit-fixtures/autonomous-execution/clean-root-cause-proceeded.txt
      .claude/audit-fixtures/autonomous-execution/flag-symptom-patch-shipped.txt

  Both carried `src/kailash/cache/keys.py` as narrative colour in a probe scenario
  — not a citation, nothing resolving against it. Fixed AT LOOM and re-cut, rather
  than allowlisted here. This repo's guard states its own criterion (allowlist only
  where a file legitimately needs SDK source: provenance lines, compliance
  tracking, navigation skills), and incidental prose meets none of them; allowlisting
  would have recorded a loom defect as a downstream exception at four consumers.
  The guard was RIGHT and is the only reason this was caught — it is a kailash-lane
  check, and the two stack-agnostic surfaces that landed first carry no such guard.

  DERIVED TREES: NOT APPLICABLE, verified against this repo's own git rather than
  inferred — AGENTS.md, GEMINI.md, .codex/, .gemini/, .coc/ are all untracked here.
  Steps 6.5/6.6/6.7 NOT APPLICABLE, not skipped.

  SDK PINS — one real bump, one apparent drift that was NOT real. Comparing against
  the BUILD repo suggested both `kailash` 2.62.0->2.63.0 and `kailash-mcp`
  0.4.3->0.5.1. `uv lock` REFUSED the first: "only kailash<=2.62.0 is available".
  The BUILD repo's `version` field is its NEXT version, not a PUBLISHED one —
  reading it as the pin target was a producer-field-vs-reader-question error
  (`instrument-discipline.md` MUST-4). `kailash` held; only `kailash-mcp` moved,
  with the lock REGENERATED rather than hand-edited.

  DISCLOSURE: CLEAN on the loom source tree (4331 files) and CLEAN on this staged
  worktree (2680 files), 0 findings each.
